@model IEnumerable<Bangazon_Workforce_Management.Models.TrainingProgramEmployee>

@{
    ViewData["Title"] = "Index";
}

<h2>Index</h2>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
                <th>
                    @Html.DisplayNameFor(model => model.TrainingProgramID)
                </th>
                <th>
                    @Html.DisplayNameFor(model => model.EmployeeID)
                </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.TrainingProgramID)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.EmployeeID)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.TrainingProgramEmployeeID">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.TrainingProgramEmployeeID">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.TrainingProgramEmployeeID">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
